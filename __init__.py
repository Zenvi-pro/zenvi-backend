"""
Zenvi Backend — FastAPI server for AI/LLM operations.

Provides chat, media analysis, search/indexing, video generation,
and management APIs. The zenvi-core frontend communicates via REST + WebSocket.

Run with:
    uvicorn main:app --host 0.0.0.0 --port 8500 --reload
    # or
    python __main__.py
"""
